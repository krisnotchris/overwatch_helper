require 'spec_helper'

describe OverwatchHelper do
  it 'has a version number' do
    expect(OverwatchHelper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
